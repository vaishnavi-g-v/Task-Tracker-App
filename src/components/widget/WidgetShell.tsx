/** Desktop widget window (Phase 7). Main window uses normal routes. */
export default function WidgetShell(): JSX.Element {
  return (
    <div className="min-h-screen bg-slate-900 p-4 text-sm text-slate-100">
      <p className="font-medium">Task widget</p>
      <p className="mt-2 opacity-70">Phase 7 UI will load here.</p>
    </div>
  );
}
